import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  viewForm: {
    paddingLeft: 25,
    paddingRight: 25,
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
    gap: 15
  },
});

export { style };
